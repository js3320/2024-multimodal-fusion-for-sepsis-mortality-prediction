[project]
name = "cxr-pipeline"
version = "0.1.0"
description = "CXR embeddings + clinical fusion pipeline"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.black]
line-length = 100

[tool.isort]
profile = "black"
